<%= form_for(@power_supply) do |f| %>
  <% if @power_supply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@power_supply.errors.count, "error") %> prohibited this power_supply from being saved:</h2>

      <ul>
      <% @power_supply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
